# ---- Core server (required to run the dashboard) ----------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
SQLAlchemy>=2.0.32
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9

# ---- Data processing ---------------------------------------------------
pandas>=2.2.2
beautifulsoup4>=4.12.3
PyMuPDF>=1.24.9

# ---- ML pipeline (optional for inference fallback; required for fine-tuning) --
torch>=2.3.1
transformers>=4.44.0
scikit-learn>=1.5.0

# ---- OCR (optional; the pipeline falls back to PyMuPDF) ----------------
azure-ai-formrecognizer>=3.3.3
